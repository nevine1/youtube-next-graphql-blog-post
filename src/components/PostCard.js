
import React from 'react'

const PostCard = ({post}) => {
  return (
    <div>
        {/* {
            post ? (
                <h2>{post.name}</h2>
                <h2>{post.excerpt}</h2>
            ):(
                <h2>Post is not found</h2>
            )
           
        } */}
        <h2> postssssssssssssssssssss</h2>
    </div>
  )
}

export default PostCard